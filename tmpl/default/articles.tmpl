<?= (!hasCategories && d.totalPages ? articlesPanel : '') ?>

<div class="digiseller-breadcrumbs">
	<a title="Магазин" href="#!digiseller/home">Магазин</a> &gt;&nbsp;
	<? if (d.breadCrumbs) {			
		for (var i = 0, l = d.breadCrumbs.length; i < l - 1; i++) { ?>
			<a title="<?= d.breadCrumbs[i].name ?>" href="#!digiseller/articles/<?= d.breadCrumbs[i].id ?>"><?= d.breadCrumbs[i].name ?></a> &gt;&nbsp;
		<? } ?>
		<strong><?= d.breadCrumbs[l - 1].name ?> (<?= d.totalItems ?>)</strong>		
	<? } ?>
</div>

<? if (hasCategories) { ?>
	<div class="digiseller-category-blocks">
		<? for (var i = 0, l = d.categories.length; i < l; i++) { ?>
			<div>			
				<a href="<?= opts.hashPrefix ?>/articles/<?= d.categories[i].id ?>">
					<img src="http://graph.digiseller.ru/img.ashx?idn=<?= d.categories[i].hasImg == 1 ? d.categories[i].id : 1?>&maxlength=<?= opts.imgsize_category ?>" alt="<?= name ?>" />
				</a>
				
				<a style="max-width:<?= opts.imgsize_category ?>px;" href="<?= opts.hashPrefix ?>/articles/<?= d.categories[i].id ?>">
					<?= d.categories[i].name ?><span>&nbsp;(<?= d.categories[i].cnt ?>)</span>
				</a>
			</div>
		<? } ?>
	</div>
	<div class="digiseller-both"></div>
<? } ?>

<?= (hasCategories && d.totalPages ? articlesPanel : '') ?>

<? if (d.totalPages || !d.totalPages && !hasCategories) { ?>
	<div class="digiseller-productList" id="<?= id ?>">
		<?= out ?>	
	</div>
	
	<div class="digiseller-articles-pager"></div>	
<? } ?>

<br />